# md-export Sample

A quick tour of what **md-export** renders into self-contained HTML.

## Text formatting

Regular text, *emphasis*, **strong**, ~~strikethrough~~, and `inline code`.
Autolinks just work: https://github.com

> A blockquote, for good measure.

## Task list

- [x] Convert GFM to HTML
- [x] Embed the stylesheet
- [ ] Take over the world

## A table

| Feature      | Supported |
|--------------|-----------|
| Tables       | yes       |
| Strikethrough| yes       |
| Task lists   | yes       |
| Autolinks    | yes       |

## Fenced code

```rust
fn main() {
    println!("Hello from a standalone HTML file!");
}
```

### Nested heading

Some closing text under a level-3 heading so the table of contents has depth.
